{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "ReallyChooseC"
  ],
  "contact": {
    "homepage": "${homepage}",
    "sources": "${sources}",
    "issues": "${sources}/issues"
  },
  "license": "${license}",
  "icon": "assets/${id}/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "cn.choosec.clientflightmod.ClientFlightMod"
    ],
    "modmenu": [
      "cn.choosec.clientflightmod.ModMenuIntegration"
    ]
  },
  "mixins": [
    "client-flight-mod.mixins.json"
  ],
  "depends": {
    "minecraft": "${minecraft_dependency}",
    "${fabric_api}": "*"
  },
  "suggests": {
  }
}
